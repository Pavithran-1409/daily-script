name: Daily Script Execution

on:
  schedule:
    - cron: '45 9 * * *'  # Runs daily at 9 AM UTC

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run Python script
        run: python AC-001.py
